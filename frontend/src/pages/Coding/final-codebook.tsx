import UnifiedCodingPage from '../../components/Coding/UnifiedCoding/unified-coding-section';

const FinalThemes = () => {
    return (
        <div>
            <UnifiedCodingPage review={true} showThemes={true} />
        </div>
    );
};

export default FinalThemes;
